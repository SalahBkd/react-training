import React from 'react';

const About = (props) => {
    // props object has routing infos by default
    //console.log(props);

    // programmatic redirect
    // setTimeout(() => {
    //     props.history.push("/");
    // }, 2000);

    return (
        <div>
            <h2>About Page !!!</h2>
        </div>
    );
};

export default About;
